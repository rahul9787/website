pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                sh 'echo "Build Successful"'
            }
        }

        stage('Deploy') {
            steps {
                sh '''
                sudo cp -r * /var/www/html/
                '''
            }
        }
    }
}
